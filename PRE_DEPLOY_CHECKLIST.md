# Чеклист перед развертыванием на сервере

## ✅ Что уже готово:

- [x] Парсер работает с Playwright
- [x] БД настроена (MySQL)
- [x] Все таблицы созданы
- [x] Поля для аренды добавлены
- [x] Прокси настроены (5 штук, 3 работают)
- [x] Docker-compose для продакшена готов
- [x] Скрипты бэкапов готовы
- [x] Инструкции по деплою готовы

## 🔍 Финальные проверки перед арендой:

### 1. Тест парсера локально (если еще не делали)

```bash
# Проверить, что парсер парсит хотя бы одно объявление
python test_full_parse.py 311739319

# Проверить, что данные сохраняются в БД
python check_rent_fields_db.py
```

### 2. Проверить ключевые файлы

- [ ] `docker-compose.prod.yml` - есть
- [ ] `Dockerfile` - есть и обновлен (с Playwright)
- [ ] `.env.example` - есть с шаблоном
- [ ] `backup_to_cloud.sh` - есть
- [ ] `DEPLOY.md` - инструкция готова

### 3. Подготовить список того, что нужно на сервере

- [ ] Ключи доступа к Yandex Object Storage (если нужны облачные бэкапы)
- [ ] Все прокси сохранены в надежном месте
- [ ] Пароли для БД придуманы

## 🚀 Можно арендовать сервер!

### Что сделать после аренды:

1. **Подключиться к серверу:**
   ```bash
   ssh root@your-server-ip
   ```

2. **Следовать инструкции из DEPLOY.md:**
   - Установить Docker
   - Загрузить проект
   - Настроить .env
   - Запустить docker-compose
   - Инициализировать БД

3. **Настроить бэкапы (опционально, но рекомендуется):**
   ```bash
   ./setup_backup_cloud.sh
   ```

## 💡 Рекомендации:

### Можно арендовать прямо сейчас, если:
- ✅ Парсер протестирован локально
- ✅ БД работает
- ✅ Есть инструкции (DEPLOY.md)

### Лучше проверить локально сначала, если:
- ❓ Еще не тестировали парсер на реальных данных
- ❓ Не уверены в работе Playwright с прокси
- ❓ Хотите протестировать сбор нескольких объявлений

## 📋 Быстрая проверка "готовности":

Выполните локально:
```bash
# 1. Тест парсера
python test_full_parse.py 311739319

# 2. Проверка сохранения в БД
python check_rent_fields_db.py

# 3. Проверка файлов
ls -la docker-compose.prod.yml Dockerfile DEPLOY.md backup_to_cloud.sh
```

Если все работает - **можно арендовать сервер!**

