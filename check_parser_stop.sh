#!/bin/bash
# Быстрая проверка причины остановки парсера после 20:00:27

echo "╔════════════════════════════════════════════════════════════╗"
echo "║    БЫСТРАЯ ДИАГНОСТИКА ОСТАНОВКИ ПАРСЕРА                    ║"
echo "╚════════════════════════════════════════════════════════════╝"
echo ""

cd /root/rentsense || exit 1

# Ограничиваем количество логов для быстрой работы
LOG_LIMIT=5000

echo "🔄 ТЕКУЩИЙ СТАТУС КОНТЕЙНЕРА:"
echo "─────────────────────────────────────────────────────────────"
docker ps --filter "name=rentsense_parser" --format "table {{.Names}}\t{{.Status}}\t{{.State}}" 2>/dev/null || echo "Контейнер не найден"
echo ""

echo "📋 ПОСЛЕДНЕЕ ДОБАВЛЕННОЕ ОБЪЯВЛЕНИЕ:"
echo "─────────────────────────────────────────────────────────────"
docker-compose -f docker-compose.prod.yml logs --tail $LOG_LIMIT parser 2>/dev/null | grep "is adding" | tail -1 || echo "Не найдено"
echo ""

echo "📋 ПОСЛЕДНИЕ СООБЩЕНИЯ О ЦИКЛАХ ПАРСИНГА (последние 10):"
echo "─────────────────────────────────────────────────────────────"
docker-compose -f docker-compose.prod.yml logs --tail $LOG_LIMIT parser 2>/dev/null | grep -E "(Начало цикла|Цикл.*завершен|пауза.*секунд|Ошибка в цикле|превысил максимальное)" | tail -10 || echo "Не найдено"
echo ""

echo "📋 ПОСЛЕДНИЕ СООБЩЕНИЯ О ЗАВЕРШЕНИИ КОМБИНАЦИЙ (последние 10):"
echo "─────────────────────────────────────────────────────────────"
docker-compose -f docker-compose.prod.yml logs --tail $LOG_LIMIT parser 2>/dev/null | grep -E "(Finished: room=|End of pglist|consecutive empty pages|Error limit|Reached page limit)" | tail -10 || echo "Не найдено"
echo ""

echo "📋 ПОСЛЕДНИЕ ОШИБКИ (последние 10):"
echo "─────────────────────────────────────────────────────────────"
docker-compose -f docker-compose.prod.yml logs --tail $LOG_LIMIT parser 2>/dev/null | grep -E "(ERROR|Exception|Traceback)" | tail -10 || echo "Ошибок не найдено"
echo ""

echo "📋 ПОСЛЕДНИЕ 20 СТРОК ЛОГОВ:"
echo "─────────────────────────────────────────────────────────────"
docker-compose -f docker-compose.prod.yml logs --tail 20 parser 2>/dev/null || echo "Логи не доступны"
echo ""

# Проверяем, когда был последний перезапуск
echo "⏱️  ВРЕМЯ РАБОТЫ КОНТЕЙНЕРА:"
echo "─────────────────────────────────────────────────────────────"
docker ps --format "{{.Status}}" --filter "name=rentsense_parser" 2>/dev/null || echo "Недоступно"
echo ""

