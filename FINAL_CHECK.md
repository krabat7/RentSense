# Финальная проверка настройки автоматического pull

## ✅ Что уже сделано:

1. ✅ SSH ключ создан
2. ✅ Remote URL изменен на SSH
3. ✅ Git pull работает
4. ✅ Скрипт обновлен

## 🔍 Финальная проверка:

Выполните на сервере:

```bash
ssh root@89.110.92.128
cd /root/rentsense

# 1. Проверить, что SSH ключ добавлен в GitHub
# Если при выполнении команды ниже видите "successfully authenticated" - все ОК
ssh -T git@github.com

# 2. Проверить cron задачу
crontab -l
# Должно быть:
# */5 * * * * /root/rentsense/git-pull-hook.sh >> /var/log/git-pull.log 2>&1

# 3. Если путь неправильный, исправить:
crontab -l | grep -v "git-pull-hook" | crontab -
(crontab -l 2>/dev/null; echo "*/5 * * * * /root/rentsense/git-pull-hook.sh >> /var/log/git-pull.log 2>&1") | crontab -

# 4. Проверить, что скрипт исполняемый
chmod +x /root/rentsense/git-pull-hook.sh
ls -la /root/rentsense/git-pull-hook.sh
# Должно быть: -rwxr-xr-x (права на выполнение)

# 5. Протестировать скрипт вручную
/root/rentsense/git-pull-hook.sh

# 6. Проверить логи
tail -f /var/log/git-pull.log

# 7. Создать тестовый коммит (на локальной машине) и подождать 5 минут
# Затем проверить, что на сервере обновилось автоматически
```

## ⚠️ Важно добавить SSH ключ в GitHub:

Если при выполнении `ssh -T git@github.com` видите ошибку аутентификации:

1. Скопируйте публичный ключ:
   ```bash
   cat ~/.ssh/id_rsa.pub
   ```

2. Добавьте в GitHub:
   - Перейдите: https://github.com/settings/keys
   - Нажмите "New SSH key"
   - Вставьте ключ
   - Сохраните

3. Проверьте:
   ```bash
   ssh -T git@github.com
   # Должно быть: "Hi krabat7! You've successfully authenticated..."
   ```

## 📊 Мониторинг:

Для просмотра логов автоматического pull:
```bash
tail -f /var/log/git-pull.log
```

## ✅ Готово!

После выполнения всех проверок автоматический pull будет работать каждые 5 минут.

