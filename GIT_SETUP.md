# Настройка Git репозитория и синхронизации с сервером

## 📋 План действий

### Шаг 1: Создание репозитория на GitHub

1. Зайдите на [GitHub.com](https://github.com)
2. Нажмите **"New repository"** (или **"+"** → **"New repository"**)
3. Заполните:
   - **Repository name**: `RentSense` (или другое имя)
   - **Description**: (опционально)
   - **Visibility**: Private (рекомендуется, т.к. есть конфигурационные файлы)
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license"
4. Нажмите **"Create repository"**
5. **Скопируйте URL репозитория** (например: `https://github.com/YOUR_USERNAME/RentSense.git`)

---

### Шаг 2: Настройка локального репозитория

**На локальной машине (Windows PowerShell):**

```powershell
# Перейдите в папку проекта
cd F:\hw_hse\Diploma\RentSense

# Проверьте, что .env файл не будет закоммичен (он уже в .gitignore)
git status

# Если Git еще не инициализирован
git init

# Добавьте удаленный репозиторий (замените URL на ваш)
git remote add origin https://github.com/YOUR_USERNAME/RentSense.git

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit: RentSense parser with photo support"

# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

**Если Git уже инициализирован:**

```powershell
# Проверьте текущий статус
git status

# Добавьте все изменения
git add .

# Закоммитьте
git commit -m "Update: исправления парсера и добавление фото"

# Отправьте на GitHub
git push origin main
```

---

### Шаг 3: Настройка сервера

**На сервере (через SSH):**

```bash
cd /root/rentsense

# Вариант А: Если код уже есть, инициализируем Git
if [ ! -d .git ]; then
    # Сохраняем .env файл (он не должен попасть в репозиторий)
    cp .env .env.backup
    
    # Инициализируем Git
    git init
    
    # Добавляем удаленный репозиторий
    git remote add origin https://github.com/YOUR_USERNAME/RentSense.git
    
    # Получаем код
    git fetch origin
    git checkout -b main origin/main
    
    # Восстанавливаем .env файл
    cp .env.backup .env
    rm .env.backup
fi

# Вариант Б: Если хотите начать с чистого листа (ОСТОРОЖНО - удалит текущие файлы!)
# cd /root
# rm -rf rentsense
# git clone https://github.com/YOUR_USERNAME/RentSense.git rentsense
# cd rentsense
# cp .env.example .env  # Создайте .env файл с вашими настройками
```

---

### Шаг 4: Скрипт для обновления на сервере

**Скопируйте `server_update.sh` на сервер:**

```bash
# На локальной машине (если есть SCP доступ)
scp server_update.sh root@89.110.92.128:/root/rentsense/server_update.sh

# Или создайте файл на сервере вручную
```

**На сервере:**

```bash
cd /root/rentsense

# Сделайте скрипт исполняемым
chmod +x server_update.sh

# Протестируйте обновление
./server_update.sh
```

---

### Шаг 5: Рабочий процесс (ежедневное использование)

**Когда вы вносите изменения локально:**

```powershell
# На локальной машине
cd F:\hw_hse\Diploma\RentSense

# 1. Проверьте статус
git status

# 2. Добавьте изменения
git add .

# 3. Закоммитьте с понятным сообщением
git commit -m "Описание изменений: что исправили/добавили"

# 4. Отправьте на GitHub
git push origin main
```

**На сервере обновите код:**

```bash
# Вариант 1: Вручную
cd /root/rentsense
./server_update.sh

# Вариант 2: Через SSH одной командой
ssh root@89.110.92.128 "cd /root/rentsense && ./server_update.sh"
```

---

### Шаг 6 (Опционально): Автоматическое обновление на сервере

**Настройте cron для автоматического обновления каждые 30 минут:**

```bash
# На сервере
crontab -e

# Добавьте строку (обновление каждые 30 минут):
*/30 * * * * cd /root/rentsense && /root/rentsense/server_update.sh >> /root/rentsense/update.log 2>&1
```

**Или через GitHub Webhook (более продвинутый вариант):**
- Настройте webhook на GitHub, который будет вызывать скрипт обновления на сервере при каждом push

---

## ⚠️ Важные замечания

1. **`.env` файл НЕ должен попадать в репозиторий** - он уже в `.gitignore`
2. **Пароли и секреты** должны быть только в `.env` файле на сервере
3. **Перед первым push** убедитесь, что в репозиторий не попадут:
   - `.env` файлы
   - Логи (`*.log`)
   - Данные (`data/`)
   - Бэкапы (`*.backup`)

## 🔍 Проверка

После настройки проверьте:

```bash
# На сервере
cd /root/rentsense
git status
git remote -v
./server_update.sh
```

---

## 📝 Примеры commit сообщений

- `"Fix: исправлена ошибка IndexError в getResponse"`
- `"Feature: добавлена поддержка сохранения фото"`
- `"Update: оптимизированы задержки между запросами"`
- `"Fix: убран ранний return без логирования"`

